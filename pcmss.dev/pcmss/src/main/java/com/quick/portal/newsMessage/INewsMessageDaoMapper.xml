<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quick.portal.newsMessage.INewsMessageDao">
	
    <!--查询条件-->
	<sql id="where">
		1=1
		<if test="resource_cd != null and resource_cd != '' and resource_cd != 'null'"><![CDATA[ 
	       and resource_cd = #{resource_cd}
	    ]]></if>
		<if test="resource_name != null and resource_name != '' and resource_name != 'null'"><![CDATA[
	       and resource_name = #{resource_name}
	    ]]></if>
		<if test='resource_status == "1" || resource_status == "0"'><![CDATA[
	       and resource_status = #{resource_status}
	    ]]></if>
		<if test="upd_time != null and upd_time != '' and upd_time != 'null'"><![CDATA[ 
	       and upd_time >= #{upd_time}
	    ]]></if>
        <if test="_sql_where != null and _sql_where != ''"> <![CDATA[
            and ${_sql_where}
        ]]> </if>
	</sql>
    
    <!-- 新增res_info -->
	<insert id="insert" parameterType="com.quick.portal.newsMessage.NewsMessageDO">
		insert into res_info (
			resource_cd,
			resource_id,
			resource_name,
			provider_cd,
			check_user,
			apply_user,
			resource_status,
			date_time,
			cre_time,
			upd_time
		)values(
			 #{resource_cd},
			 #{resource_id},
			 #{resource_name},
			 #{provider_cd},
			 #{check_user},
			 #{apply_user},
			 #{resource_status},
			 #{date_time},
			 #{cre_time},
			 #{upd_time}
			);
		<selectKey resultType="int" order="AFTER" keyProperty="resource_cd">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
		
	<!-- 修改res_info -->
	<update id="update" parameterType="com.quick.portal.newsMessage.NewsMessageDO">
		update res_info 
		   set resource_cd=#{resource_cd}
			  ,resource_id=#{resource_id}
			  ,resource_name=#{resource_name}
			  ,provider_cd=#{provider_cd}
			  ,check_user=#{check_user}
			  ,apply_user=#{apply_user}
			  ,resource_status=#{resource_status}
			  ,date_time=#{date_time}
			  ,upd_time=#{upd_time}
		 where resource_cd = #{resource_cd}
	</update>
    
	<!-- 删除 -->
	<delete id="delete" parameterType="String">
		delete from res_info where resource_cd = #{resource_cd};
	</delete>
    
    <!--查询-->
	<select id="select" parameterType="map" resultType="map">
		select * from res_info
		<where>
			<include refid="where" />
		</where>
        <if test="_sql_order != null and _sql_order != ''">
            order by ${_sql_order}
        </if>
	</select>
	<!--查询消息状态是否阅读-->
	<select id="messageStatu" resultType="map">
		select * from res_info where resource_status = 0
        <if test="_sql_order != null and _sql_order != ''">
            order by ${_sql_order}
        </if>
	</select>
	
	<!-- 查询总记录数 -->
	<select id="count" parameterType="map" resultType="int">
		select count(*) from res_info
		<where>
			<include refid="where" />
		</where>
	</select>

</mapper>
